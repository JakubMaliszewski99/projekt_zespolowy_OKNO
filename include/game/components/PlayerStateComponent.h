#pragma once

struct PlayerStateComponent{
    bool isMovingForward;
    bool isMovingBackwards;
    bool isMovingLeft;
    bool isMovingRight;
    bool isRotatingLeft;
    bool isRotatingRight;
    bool isZoomingIn;
    bool isZoomingOut;
    bool isFiring;
    bool isAlive;
};